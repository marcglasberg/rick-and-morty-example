/**
 * Mock for file imports
 * 
 * This mock is used by Jest when importing non-JavaScript files like images,
 * fonts, videos, etc. It returns an empty string which is enough for most tests.
 */
module.exports = '';
